import Chart from "../chart";

export default class Bar extends Chart {

    constructor(el: HTMLElement, elProperties: Record<string, any>) {
        super(el, elProperties)
    }

    public render() {

    }

    public rerender() {

    }
}
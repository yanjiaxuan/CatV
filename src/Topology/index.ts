import CatV from "../chart";

export default class Topology extends CatV {

    constructor(el: HTMLElement, elProperties: Record<string, any>) {
        super(el, elProperties)
    }

    public render() {

    }

    public rerender() {

    }
}